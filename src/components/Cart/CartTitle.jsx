// eslint-disable-next-line react/prop-types
const CartTitle = ({ title }) => {
  return (
    <div className="font-montserrat font-semibold text-2xl mb-6">
      {title}
    </div>
  );
};

export default CartTitle;
