// eslint-disable-next-line react/prop-types
const CartTitle = ({ title }) => {
  return (
    <div className="font-montserrat font-semibold text-lg md:text-2xl mb-4">
      {title}
    </div>
  );
};

export default CartTitle;
