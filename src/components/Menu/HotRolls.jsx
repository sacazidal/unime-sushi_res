import Sort from "../Sort";
import SectionWrapper from "./SectionWrapper";

const HotRolls = () => {
  return (
    <SectionWrapper id={"горячие-роллы"}>
      <Sort title="Горячие роллы" />
    </SectionWrapper>
  );
};

export default HotRolls;
