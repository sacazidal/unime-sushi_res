import Sort from "../Sort";
import SectionWrapper from "./SectionWrapper";

const Snacks = () => {
  return (
    <SectionWrapper id={"закуски"}>
      <Sort title="Закуски" />
    </SectionWrapper>
  );
};

export default Snacks;
