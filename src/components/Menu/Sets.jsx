import Sort from "../Sort";
import SectionWrapper from "./SectionWrapper";

const Sets = () => {
  return (
    <SectionWrapper id={"сеты"}>
      <Sort title="Сеты" />
    </SectionWrapper>
  );
};

export default Sets;
