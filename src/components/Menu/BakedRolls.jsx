import Sort from "../Sort";
import SectionWrapper from "./SectionWrapper";

const BakedRolls = () => {
  return (
    <SectionWrapper id={"запеченные-роллы"}>
      <Sort title="Запеченные роллы" />
    </SectionWrapper>
  );
};

export default BakedRolls;
